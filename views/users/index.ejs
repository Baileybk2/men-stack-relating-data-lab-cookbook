<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Community Page</title>
  </head>
  <body>
    <h1>Explore Our Community</h1>

    <%- console.log("MY OBVIOUS MESSAGE", users) %>
    <ul>
      <% users.forEach((user) => { %>
      <li><%= user.username %></li>
      <% }); %>
    </ul>
  </body>
</html>
